exports.show = (id)=> {

    // lógica com o Banco
    // regras de negócio

    const resultado = {
        mensagem: 'Isso é um recurso com parâmetros vindo do controller',
        id: id
    }

    return resultado
}